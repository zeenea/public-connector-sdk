package zeenea.connector.common.filter;

/**
 * Enum representing the type of rule that can be applied in a filter. The rule can either be a glob
 * pattern or a regular expression.
 */
public enum RuleType {
  GLOB,
  REGEX
}
