package zeenea.connector.common.filter;

/**
 * Enum representing the action to be taken when a filter is applied. The action can either be to
 * accept or reject the items that match the filter criteria.
 */
public enum FilterAction {
  ACCEPT,
  REJECT
}
